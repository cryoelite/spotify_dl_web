import 'package:flutter/material.dart';

class ManualDownloadBox extends StatelessWidget {
  final double width;
  final double height;
  const ManualDownloadBox({Key? key, required this.width, required this.height})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
